import { styled } from "goober";

export const Wrapper = styled('nav')`
    background-color: var(--regexbar);
    color: var(--white);
    align-items: center;
    justify-content: space-between;
    z-index: 1;
    position: relative;
    padding: 20px;
    border: 1px solid var(--black);
`